Collection of short scripts:

1. *camera.py*: used to acquire images to build the dataset
2. *image-cropper.py*: used to crop the acquired images so to fit the MobileNetV2 input size.
3. *label-maker.py*: script to produce the annotation files for the training of the model.
